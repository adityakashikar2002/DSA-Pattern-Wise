// Time: O(n)
// Space: O(1)
---------------------------------------------------------------------------------------------------------------
---------------------------------------------------------------------------------------------------------------

/*
    simple baat hai jo bhi 1st computer hai label - 0
    vo to unlocked hi hai

    agar next computers ki list me koi ek bhi computer aya jiska
    complexity[i] <= complexity[0] hua to return 0 kr denge
    kyuki shayad baki k kuch computers unlock ho, lekin sare computers unlock nhi honge like

    complexity = [3,3,3,4,4,4]
    label =      [0,1,2,3,4,5]

    label_0 = 3 (unlocked hai)
    label_1,2 (3 <= 3) unlock nhi honge
    jabki label_3,4,5  inki complexity > complexity[0] hai yee unlock honge

    so, kyuki sare computers unlock nhi hua hum return 0 kr denge

    aur dusri baat yee observation milta hai ki 
    koi bhi list jo ascending order me hai aur usme unique elements hai vo contribute kregi ans me
    lekin kaise ??

    simple baat hai sare unlock honge
    arrangement me 1st computer label-0 to fix hai

    to har baar hamare paas har position pr rahne k liye currently bache hue elements honge like

    1, n - 1, n - 2, ... so on

    like example

    [1, 2, 3, 4]
    [1, 3, 2, 1]

    count = 3! (n - 1)! is the answer always

*/
class Solution {
    public int countPermutations(int[] complexity) {
        int n = complexity.length;
        int mod = 1_000_000_007;

        long count = 1;

        for(int i = 1; i < n; i++)
        {
            if(complexity[i] <= complexity[0])
                return 0;
            
            count = (count * i) % mod;
        }

        return (int) count;
    }
}
