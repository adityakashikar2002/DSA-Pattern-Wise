// Time: O(n)
// Space: O(1)
-----------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------
/*
    simple idea hai, 

    nums[0...p] is strictly increasing,
    nums[p...q] is strictly decreasing,
    nums[q...n − 1] is strictly increasing.

    yee conditions honi hi chaiye, so agar kahi point pr yee fail hoti hai 
    ya array already end ho jata hai without fulfilling these conditions to sidha
    return false
*/
class Solution {
    public boolean isTrionic(int[] nums) {
        int n = nums.length;
        int i = 0;

        // increasing check
        while(i + 1 < n && nums[i] < nums[i + 1])
            i++;
        if(i == 0 || i == n - 1)
            return false;

        // decreasing check
        while(i + 1 < n && nums[i] > nums[i + 1])
            i++;
        if(i == 0 || i == n - 1)
            return false;

        // increasing check
        while(i + 1 < n && nums[i] < nums[i + 1])
            i++;
        if(i == n - 1)
            return true; 

        return false;    
    }
}
