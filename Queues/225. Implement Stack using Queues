// Brute Force - Using Two Queues
// Time: O(n) [ for push operation]
// Space: O(n)
------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
/**
 * Ye class Stack ko implement karti hai do Queues ka use karke.
 * 
 * Intuition:
 * Stack me last me dalne wala element sabse pehle bahar aata hai (LIFO).
 * Queue me pehle dalne wala element pehle bahar aata hai (FIFO).
 * 
 * Isliye jab bhi hum push karte hain, hum naya element q1 me dalte hain
 * aur q2 ke saare purane elements q1 me move kar dete hain.
 * Fir q1 aur q2 ko swap kar dete hain.
 * 
 * Result:
 * q1 ka front hamesha stack ka top ban jata hai.
 */
class MyStack {
    Queue<Integer> q1;
    Queue<Integer> q2;

    public MyStack() {
        q1 = new LinkedList<>();
        q2 = new LinkedList<>();
    }
    
    public void push(int x) {
        q1.add(x);

        while(!q2.isEmpty())
        {
            q1.add(q2.remove());
        }

        Queue<Integer> tempQ = q1;
        q1 = q2;
        q2 = tempQ;

    }
    
    public int pop() {

        return q2.remove();
    }
    
    public int top() {
        return q2.peek();
    }
    
    public boolean empty() {
        return q1.isEmpty() && q2.isEmpty();
    }
}

/**
 * Your MyStack object will be instantiated and called as such:
 * MyStack obj = new MyStack();
 * obj.push(x);
 * int param_2 = obj.pop();
 * int param_3 = obj.top();
 * boolean param_4 = obj.empty();
 */
------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
// Optimal Approach - Using Single queue
// Time: O()
// Space: O()
------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
